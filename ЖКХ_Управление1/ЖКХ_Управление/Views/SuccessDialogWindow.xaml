<Window x:Class="ЖКХ_Управление.Views.SuccessDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Успех" Height="180" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="White">

    <Border BorderThickness="2" BorderBrush="#4CAF50" CornerRadius="10" Margin="10">
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
                <TextBlock Text="✔" FontSize="28" Foreground="#4CAF50" Margin="0,0,10,0"/>
                <TextBlock x:Name="MessageText" FontSize="16" Foreground="Black" TextWrapping="Wrap"/>
            </StackPanel>
            <Button Content="ОК"
                    Width="80"
                    Height="30"
                    HorizontalAlignment="Right"
                    Background="#4CAF50"
                    Foreground="White"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Border>
</Window>
